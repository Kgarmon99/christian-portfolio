{
  "name": "agent-command-center",
  "version": "2.0.0",
  "private": true,
  "scripts": {
    "build": "mkdir -p dist && cp -r public/* dist/"
  },
  "dependencies": {
    "@vercel/edge-config": "^1.4.0"
  }
}
